{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "npm install --force && @vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    }
  ]
}